// rfid_bridge_secrets.h.example -- шаблон секретов прошивки (design E4/E5).
//
// Скопируй в rfid_bridge_secrets.h (тот файл в .gitignore, не коммитить)
// и подставь реальные значения перед прошивкой -- см. README.md рядом.

#pragma once

// Секрет, продублированный в конфиге узла (config/operator_ui.yaml ->
// rfid_secret_file, вне git) -- HMAC-SHA256(secret, nonce) подписывает
// КАЖДЫЙ ответ ESP. Доказывает узлу, что ответ пришёл от легитимной
// прошивки, а не был инжектирован в serial-линию (design E4: "секрет не
// покидает ESP, наружу уходит только подпись"). Минимум 32 случайных
// байта, например: `openssl rand -hex 32`.
#define RFID_BRIDGE_SHARED_SECRET "REPLACE-ME-shared-secret-min-32-bytes-openssl-rand-hex-32"

// Ключ сектора MIFARE (Key A), которым прошиты настоящие карты
// операторов -- НЕ заводской 0xFFFFFFFFFFFF (его знает любой Proxmark и
// любой блокнот в интернете). Карта без этого ключа не аутентифицируется
// вообще -- расценивается как "нет карты", а не читается по UID
// (design E4: "аутентификация карты -- по секрету защищённого сектора,
// не по UID").
#define RFID_BRIDGE_SECTOR_KEY \
  { 0x11, 0x22, 0x33, 0x44, 0x55, 0x66 }

// Серийный номер USB-дескриптора ЭТОЙ КОНКРЕТНОЙ платы -- обязателен
// явно (design E5): у ESP32-S3 нативный USB, дефолтный VID:PID
// 303a:1001 общий для тысяч устройств, и без явного серийника udev не
// сможет отличить один ридер от другого. Уникальный на весь парк роботов,
// не только на одну плату.
#define RFID_BRIDGE_USB_SERIAL "guide-robot-rfid-01"
