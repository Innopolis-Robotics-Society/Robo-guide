# Параметры face_node. Порт 8090 на Jetson (10.100.20.169) свободен.

/**/face_node:
  ros__parameters:
    http_host: "0.0.0.0"
    http_port: 8090
    web_root: "$(find-pkg-share guide_robot_face)/web"
    states_yaml: "$(find-pkg-share guide_robot_face)/config/face_states.yaml"
