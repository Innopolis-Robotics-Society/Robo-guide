# Открыть логический PCM-поток для одной активной Say-цели.

string goal_id

# Пустая строка разрешена при первом подключении. Непустое значение защищает
# клиента от незаметного продолжения после переподключения USB/audio owner.
string expected_device_session_id

# Зарезервировано для voice_session_manager. До этапа admission пусто.
string admission_token

uint32 sample_rate
uint16 channels
---
bool accepted
string device_session_id
uint64 stream_id
uint64 generation
uint32 max_pcm_samples
string reason
